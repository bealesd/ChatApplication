TODO:

add azure repo for storing messages (as opposed to static class)
azure repo must exist, i.e. the table, the resource group, if not this program must create it
imporve how to select who is chatting
add a password to login that is checked against a one way hash
lock the actions until login is confirmed
display only the last 10 message
scrolling to see more message, or action for viewing older message by page indexes